{
  "type": "object",
  "$id": "schema:call:add:body",
  "required": ["audio_url"],
  "additionalProperties": false,
  "properties": {
    "audio_url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://.*\\.(wav|mp3)$"
    }
  }
}
